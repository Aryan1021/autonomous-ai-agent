services:
  autonomous-ai-agent:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: autonomous-ai-agent

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env